{#
/**
 * @file
 * Contains the text of the page_example explanation page
 */
#}

{% set eventsreg_simple = path('eventsreg.simple') %}
{% set eventsreg_arguments = path('eventsreg.arguments', {'first': 23, 'second': 56}) %}

{% trans %}
<p>The Page example module provides two pages, "simple" and "arguments".</p>
<p>The <a href={{ eventsreg_simple }}>simple page</a> just returns a renderable array for display.</p>
<p>The <a href={{ eventsreg_arguments }}>arguments page</a> takes two arguments and displays them, as in {{ eventsreg_arguments }}</p>
{% endtrans %}
